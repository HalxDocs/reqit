reqit v0.6.1 is out 🚀

• Virtual scrolling — CollectionsTree handles 1000+ requests without lag
• Drag-and-drop reorder collections & requests with visual drop indicators
• Batch select / move / delete multiple requests at once
• Markdown API docs export with configurable toggles
• CI/CD pipeline generation (GitHub Actions + GitLab CI)
• Test suite runner integration — run suites directly
• Configurable concurrent request limit

github.com/HalxDocs/reqit

#reqit #api #testing #devtools
