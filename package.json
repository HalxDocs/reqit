{
  "name": "reqit-web",
  "private": true,
  "scripts": {
    "postinstall": "cd flux/frontend && npm install && npm run build:web && cp -r dist-web/* ../../ && cp dist-web/web.html ../../index.html && rm -rf node_modules",
    "start": "node serve.js"
  }
}
