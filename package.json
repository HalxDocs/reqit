{
  "name": "reqit-web",
  "private": true,
  "scripts": {
    "build": "cd flux/frontend && npm install && npm run build && cp -r dist/* ../",
    "start": "serve -s -l 3000"
  },
  "dependencies": {
    "serve": "^14.2.4"
  }
}
